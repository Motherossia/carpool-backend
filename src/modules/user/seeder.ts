// import { seeder } from 'nestjs-seeder';
// import { ProductSeeder } from './user.seeder';
// import { MongooseModule } from '@nestjs/mongoose';
// import { User, UserSchema } from '../../entities/user.entity';

// seeder({
//     imports: [MongooseModule.forRoot('mongodb://localhost:27017/NEST_TEST'), MongooseModule.forFeature([{ name: 'User', schema: UserSchema }])],
// }).run([ProductSeeder]);
