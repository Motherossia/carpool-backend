export enum TrackStatusEnum {
    DRAFT = 'DRAFT',
    PUBLISHED = 'PUBLISHED',
    ARCHIVED = 'ARCHIVED',
}

export enum episodeTypeEnum {
    PRIVATE = 'PRIVATE',
    PUBLIC = 'PUBLIC',
}
