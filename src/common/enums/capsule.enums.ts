export enum CapsuleStatusEnum {
    CREATED = 'CREATED',
    TAKEN = 'TAKEN',
    UNCOMMITTED = 'UNCOMMITTED',
}

export enum CapsuleTypeEnum {
    TRACK_CAPSULE = 'TRACK_CAPSULE',
    SD_CAPSULE = 'SD_CAPSULE',
}
